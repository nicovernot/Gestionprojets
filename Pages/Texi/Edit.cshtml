@page
@model Medecins.Pages.Texi.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>TypeExigeance</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TypeExigeance.TypeExigeanceID" />
            <div class="form-group">
                <label asp-for="TypeExigeance.nom" class="control-label"></label>
                <input asp-for="TypeExigeance.nom" class="form-control" />
                <span asp-validation-for="TypeExigeance.nom" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Retour à la liste</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
